{% autoescape false %}
    {{ includeAssets({"type": "css"}) }}
    {{ includeAssets({"type":"js"}) }}

    <script>
        // $ may not be defined
        console.log(document.head.innerHTML);
        window.$ = window.jQuery;
        console.log(window.$);
    </script>
{% endautoescape %}
